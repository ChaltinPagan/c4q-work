var string = window.prompt("Enter a string.");

var input = window.prompt("Enter a number.");

number = Number(input)

var newStr = "";

for (var i = 1; i <= number; i++) {
    newStr += string + " "
    newStr
}


window.alert(newStr);